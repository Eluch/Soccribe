version: '3'
services:
    soccribe:
        network_mode: bridge
        ports:
            - '8080:80'
        volumes:
            - './:/usr/src/app/'
        container_name: soccribe
        restart: always
        build: .
